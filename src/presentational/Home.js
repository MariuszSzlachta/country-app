import React from 'react';

const home = props => (
  <div className="home">
    <h1 className="home__title">Welcome</h1>
    <p className="home__text">let's take a journey arount the countries</p>
  </div>
)

export default home;